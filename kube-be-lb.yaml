apiVersion: v1
kind: Service
metadata:
  name: kube-be-lb
  namespace: test-kube-app
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: kube-be
