apiVersion: v1
kind: Service
metadata:
  name: kube-py-lb
  namespace: test-kube-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: kube-py
